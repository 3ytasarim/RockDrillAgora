node_modules
dist
.DS_Store
.env
.env.local
.env.*.local
server/public
vite.config.ts.*
*.tar.gz
# local SEO audit scratch
.seo-audit/

# product images (rsync to server on deploy, not in git)
product-images/
